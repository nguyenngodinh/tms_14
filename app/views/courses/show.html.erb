<% provide(:title, @course.name) %>
 
<div class="row">
	<div> 
  <section class="microposts">
      <%= @course.name %>
  </section>	  
</div>
<div> 	
  	<h3><%= @course.content%></h3>  
</div>
<div class="row">
	<div class="col-md-6">
		<%= link_to "delete", @course, method: :delete,
                                  data: { confirm: "You sure?" } %>
        <%= link_to "edit", edit_course_path(@course) %>
        <%= link_to "create", new_course_path %>
	</div>
</div>
		
